namespace WebApplication1.AppMetaData
{
    public partial class Router
    {
        private const string root = "Api";
        private const string version = "V1";
        protected const string Rule = root + "/" + version + "/";
    }
}
